必须掌握的：
1.去github注册账户然后新建代码库
2.git clone 地址 下载项目
3.git add. 提交代码第一步
4.git commit -m 'test' 提交代码第二步
5.git push 真正上传代码
6 git pull 更新代码



通过git版本管理(master)，比如每一个任务都有一个分支版本，每一个开发者会在分支版本的基础上新建一个自己的版本，在提交代码之前首先更新分支任务的最新代码，然后把代码合并到自己的版本，再提交自己的代码，从而保证自己的代码不会影响到别人的代码。
